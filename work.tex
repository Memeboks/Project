\section{ѕостановка задачи}

Ќа плоскости заданы множество точек ј и точка d вне его. ѕодсчитать количество (неупор€доченных) различных троек точек a,b,c из ј таких, что четырехугольник abcd €вл€етс€ параллелограммом.

\section{јлгоритм решени€ задачи}

ѕеребираем три точки a, b и с из заданного множества A, и провер€ем, подход€т ли они дл€ решени€ нашей задачи.

\subsection{Ѕазовые структуры данных}

—труктурой данных €вл€етс€ точка-"Point".

ѕол€ класса: координаты точек X и Y(остальные пол€ будут добвленны в процессе решени€ задачи).

\subsection{ѕостроение алгоритма}

1)Ѕерем по пор€дку элемент A из множества м (м может быть обычный двумерный массив, в котором кажда€ строка(столбец) хранит координаты одной точки на плоскости)

¬ычисл€ем проекции этого отрезка на оси х и у и будем хранить их в переменных ’ и ” соответсвенно (их можно и не вычисл€ть тут, а следовательно, и не заводить новые переменные, но данный вариант уменьшит количество вычислений, которые нам бы пришлось произвести)

2)Ѕерем по пор€дку элемент B из множества м

≈сли B не равно A то (то есть это не одна и та же точка, ведь множество у нас одно)

3)Ѕерем по пор€дку элемент C из множества м

≈сли B не равно C, и C не равно A то (провер€ем не €вл€етс€ ли наша нова€ точка точкой A или C, ведь множество у нас одно)

ѕроверим, €вл€етс€ ли отрезок BC равный отрезку AD через из их проекции на соответсвующие оси и €вл€ютс€ ли они параллельными и не лежащими на одной пр€мой(если да, то) признаем наш параллелограмм